# Frida Upload

Upload frida-server:

```bash
./androidlab.sh frida android-emu13-nostore /path/to/frida-server
```

Upload and start:

```bash
./androidlab.sh frida android-emu13-nostore /path/to/frida-server --start
```

The helper pushes to:

```text
/data/local/tmp/frida-server
```
