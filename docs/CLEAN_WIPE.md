# Clean and Wipe

Clean all pods, keep data:

```bash
./androidlab.sh clean-all
```

Clean all and wipe data:

```bash
./androidlab.sh clean-all --wipe
```

Delete one emulator:

```bash
./androidlab.sh delete NAME
```

Delete and wipe one emulator:

```bash
./androidlab.sh delete NAME --wipe
```
